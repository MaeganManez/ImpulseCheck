// Override API base URL here when deploying to a different environment.
// On Vercel: set VITE_API_BASE (or similar) and inject it via a build step,
// or simply update this value before deploying.
window.IC_API_BASE = 'https://impulsecheck-backend.onrender.com/api';
